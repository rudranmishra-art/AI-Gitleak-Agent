repos:
  - repo: local
    hooks:
      - id: gitleaks
        name: Detect hardcoded secrets
        entry: gitleaks protect --staged --verbose --config config/gitleaks.toml
        language: system
        pass_filenames: false